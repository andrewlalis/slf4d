# Version 4.2.0

- Added `parseLoggingLevel(string s)` to parse a logging level from an arbitrary
  string.
- Added `LoggingException`, specifically for the case where parsing a logging
  level fails.